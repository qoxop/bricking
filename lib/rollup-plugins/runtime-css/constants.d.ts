export declare const Virtual_Inject_Module_Id = "\0runtime-style-inject";
export declare const Entry_Css_Str_Tpl = "__entry_css$file_tpl__";
export declare const Virtual_Inject_Module_Code = "\nconst handledIds = {};\nconst injectFnMap  = {\n    link({relativeUrl}) {\n        const linkUrl = new URL(relativeUrl, import.meta.url).href;\n        const link = document.createElement('link');\n        link.href = linkUrl;\n        link.type = 'text/css';\n        link.rel = 'stylesheet';\n        document.head.appendChild(link);\n    },\n    inline({cssVariableName}) {\n        const style = document.createElement('style');\n        style.innerText = cssVariableName;\n        document.head.appendChild(style);\n    }\n};\nexport default function({ cssVariableName, relativeUrl, id, type }) {\n    if(!handledIds[id]) {\n        if (injectFnMap[type]) {\n            injectFnMap[type]({cssVariableName, relativeUrl});\n            handledIds[id] = true;\n        }\n    }\n}\n";
export declare const Entry_Css_Str_Tpl_Code: string;
