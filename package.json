{
	"type": "module",
	"scripts": {
		"build": "turbo build",
		"test": "turbo test",
		"lint": "turbo lint",
		"deploy": "npx changeset status && changeset publish"
	},
	"dependencies": {
		"@changesets/cli": "^2.26.1",
		"turbo": "^1.10.1"
	}
}
